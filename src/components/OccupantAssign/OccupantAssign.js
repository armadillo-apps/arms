import React from "react";
import SearchBar from "../SearchBar/SearchBar";

const OccupantAssign = ({ occupants }) => {
  return (
    <React.Fragment>
      <SearchBar placeholder="occupants here..." />
    </React.Fragment>
  );
};

export default OccupantAssign;
